# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

#################
# BASE SETTINGS #
#################

# use inconsolata for window titles
font pango:InconsolataGo Nerd Font Mono 11

# use these keys for focus, movement, and resize directions
set $up k
set $down j
set $left h
set $right l

# set $mod to Alt and $alt to super
set $mod Mod1
set $alt Mod4

# use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# reload the configuration file
bindsym $alt+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $alt+r restart

# exit i3 (logs you out of your X session)
bindsym $alt+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

